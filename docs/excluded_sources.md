# Excluded sources

Sources considered and rejected because they are licensed, paywalled, require
credentials, or otherwise violate the "public, unlicensed data only" rule.
Format:

`Source | Section it would have served | Reason excluded | Date`

| Source | Would have served | Reason excluded | Date |
|---|---|---|---|
| Intex | Section 3/5 (deal-level cashflow/waterfall detail) | Licensed, credentialed platform | 2026-07-08 |
| LCD / PitchBook | Section 4/5 (loan/CLO market commentary and data) | Subscription required | 2026-07-08 |
| Creditflux | Section 5 (CLO news/data) | Subscription required | 2026-07-08 |
| Bloomberg Terminal/BVAL | Any section needing pricing | Licensed terminal access | 2026-07-08 |
| Morningstar (behind login) | Section 2 (fund data) | Paywalled beyond free summary pages | 2026-07-08 |
| `ishares.com` full-holdings ajax CSV for CLOA/CLOD | Section 1 (ETF holdings) | Not paywalled, but the classic `<id>.ajax?fileType=csv` endpoint returns an HTML shell rather than data for these products, and the product page's real holdings widget loads via a client-rendered JS component with no discovered stable JSON endpoint. Not a licensing issue — a JS-rendering wall. Left unresolved rather than adding headless-browser automation. | 2026-07-08 |
| `vaneck.com` CLOB/CLOI holdings pages | Section 1 (ETF holdings) | Plain HTTP GET loops on a cookie-consent redirect (`?cken=true`) and never reaches a data page without executing JS/setting the consent cookie via a real browser. | 2026-07-08 |
| `invesco.com` ICLO product page | Section 1 (ETF holdings) | Returns HTTP 406 to a scripted client regardless of Accept/User-Agent headers tried. | 2026-07-08 |
| `clozfund.com` (Eldridge, fka Panagram, CLOZ) | Section 1 (ETF holdings) | Only quarterly holdings PDFs found (`Eldridge-CLOZ-Q1.pdf`, `-Q3.pdf`); no daily CSV/HTML table. Not excluded for licensing — just no daily granularity available. Presale-style PDF extraction could recover quarterly snapshots later if needed. | 2026-07-08 |

| SIFMA US ABS/issuance statistics | Section 4 (issuance cycle) | The download link on sifma.org routes through an HubSpot lead-gen form (`share.hsforms.com`), not a direct file; a previously-known direct `.xlsx` path under `wp-content/uploads` now 404s (site rebuilt on Next.js). Automating a marketing lead-capture form isn't "polite public scraping" in spirit, so this is treated as gated rather than public, even though there's no paywall per se. | 2026-07-09 |

| S&P Global Ratings (spglobal.com/ratings) | Section 5 (rating actions, presales) | Entire domain sits behind Akamai bot protection — even `robots.txt` itself returns HTTP 403 to a scripted client with a declared, honest User-Agent. **Retried 2026-07-09** with a browser-form User-Agent and three alternate entry points (`disclosure.spglobal.com` regulatory-disclosure subdomain, `standardandpoors.com` legacy domain, a topics/CLO research path) — every path either 403s directly or redirects back into the same walled `spglobal.com/ratings` domain. Confirmed hard block, not a login wall. |  2026-07-08; retried 2026-07-09 |
| Fitch Ratings (fitchratings.com) | Section 5 (rating actions, presales) | Reachable (200, permissive robots.txt) but the research/rating-action listing pages are entirely client-rendered. **Retried 2026-07-09**: found `sitemap-research.xml`, which lists thousands of real, dated individual rating-action URLs (e.g. `fitch-rates-carlyle-us-clo-2017-2-ltd-refinancing-notes-08-07-2026`) — genuinely new discovery. But the individual pages are *also* client-rendered: the fetched HTML (~1MB) contains zero occurrences of the article's own subject-company name or "CLO" anywhere in the markup; only page-shell CSS/JS loads server-side, confirmed via a `__NEXT_DATA__`/embedded-JSON search that came up empty. Getting the actual text would require executing JS (a headless browser), which is a different technical approach — not attempted without checking in first, since it means adding a new dependency (Playwright/Selenium) outside the approved list. Fitch's own docs also confirm programmatic access ("Feeds and API") is a paid product. | 2026-07-09; retried same day |

| IMF Global Financial Stability Report | Section 6 (regulatory alarm index) | `imf.org/en/Publications/GFSR` redirects (HTTP 307) to a page structure this project couldn't map to a stable per-chapter PDF URL pattern in the time available. **Retried 2026-07-09** with a browser-form User-Agent against the main pub page, `elibrary.imf.org`, and a direct `/media/Files/...` file path — every one returns HTTP 403 from a server identifying as `AkamaiGHost`, the same bot-protection vendor blocking S&P. Confirmed hard block, not a missed discovery step. | 2026-07-09; retried same day |
| ECB Financial Stability Review (historical archive) | Section 6 (regulatory alarm index) | **Retried 2026-07-09**, resolved further than the first pass: the index page's server-rendered HTML actually does link 5 dated issues (May 2024 through May 2026) with their real hash-suffixed PDF URLs, which were extracted and are now scraped (see `config.ECB_FSR_REPORTS` and `docs/sources.md`). What remains excluded is the *older* archive (pre-2024) — those years don't appear in the static page source and seem to load via further JS pagination not resolved this pass. | 2026-07-08; partially resolved 2026-07-09 |
| Earnings-call transcripts (CLO CEFs, alt managers) | Section 6 (insider tone) | Not gated — genuinely bespoke per issuer (each IR site hosts replays/transcripts differently: PDF 8-K exhibits, third-party HTML, audio-only) with no shared structure like BDC SOI's XBRL fragments to exploit. Would need one parser per company; out of scope for remaining time budget. | 2026-07-09 |
| Reddit (r/investing, r/bonds, etc.) | Section 6 (retail sentiment) | Not gated in the traditional sense — this is a personal-API-credential source per the mission brief (PRAW with the user's own Reddit app), and no `REDDIT_CLIENT_ID`/`REDDIT_CLIENT_SECRET`/`REDDIT_USER_AGENT` were present in this run's environment. Scraper is fully implemented in `src/sentiment/scrape_reddit.py`; runs for real once those are exported. | 2026-07-09 |
| ICI weekly money-market fund AUM (`ici.org/weekly-money-market-mutual-fund-assets`) | Macro opener (cash-on-the-sidelines demand kicker) | Not paywalled, but the page's only stable download is `mm_summary_data_<year>.xls`, a legacy binary BIFF8 workbook requiring `xlrd` (not in `requirements.txt`); the other linked `.xlsx` on that page is an unrelated ICI report (401(k) plan economics), not MMF assets. Adding a dependency needs a check-in first per project policy, and this is an appendix-tier exhibit, not one of the two synthesized slides, so it's stubbed rather than pursued further this pass. | 2026-07-11 |
| SIFMA statistics pages, re-checked for the macro market-size comparison | Macro opener (CLO vs HY/IG/muni/Treasury/agency-MBS scale chart) | Re-verified 2026-07-11 against `docs/excluded_sources.md`'s prior 2026-07-09 finding: `sifma.org/resources/research/*` now 308-redirects into an entirely client-rendered Next.js app (`robots.txt` itself is JS-shell HTML); no server-rendered data reachable without executing JS. Still gated, not re-resolved. Market-size comparators sourced from FRED-mirrored Fed/Treasury series instead (`config.MACRO_MARKET_SIZE_FRED`); CLO's own outstanding figure reuses Section 4's dated Fed FEDS-note citation (`config.FED_CLO_HOLDER_CITATION`) rather than a fabricated current number. | 2026-07-11 |

| GDELT DOC 2.0 API (`api.gdeltproject.org`) | Section 6 v2 (high-frequency attention/tone backbone) | Not gated/paywalled — a free, keyless public API — but persistently returned HTTP 429 from this project's sandboxed egress even at 8-24s spacing (GDELT's own stated policy is 1 request/5s) and even through `CachedSession`'s full 5-attempt exponential backoff (up to ~46s total). Likely a shared-IP quota this sandbox's network sits behind, not a genuine per-request rate limit. `scrape_gdelt.py` is fully implemented and degrades gracefully per-query (a persistent-retry failure on one query no longer kills the run); it will produce real data the moment it's run from an environment with normal GDELT access. News-headline volume (`scrape_news_rss.py`, `scrape_yf_news.py`) stands in as the attention-backbone proxy in the meantime. | 2026-07-11 |

_(This list grows as individual scrapers hit a paywall/login wall; each gets logged here rather than worked around.)_
